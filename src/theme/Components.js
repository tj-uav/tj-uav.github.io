import styled from "styled-components"

const horizontalContainerStyles = {
    display: "flex",
    flexWrap: "wrap",
    width: "100%",
    margin: "0",
    padding: "0"
}

export const HorizontalContainer = styled.div(() => horizontalContainerStyles);